# Frontend environment variables template
# Copy this file to frontend/.env.local and fill in the actual values.
# Only expose values prefixed with NEXT_PUBLIC_ — everything else belongs on the backend.

# Base URL for the backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: branding tweaks
NEXT_PUBLIC_APP_NAME="Resume Agent"


