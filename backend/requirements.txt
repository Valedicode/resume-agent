# FastAPI and server
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# LangChain and LangGraph
langchain>=1.0.0
langgraph>=1.0.0
langchain-openai>=1.0.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# OpenAI SDK (for Audio API)
openai>=1.0.0

# PostgreSQL support for LangGraph checkpointing
langgraph-checkpoint-postgres>=1.0.0
psycopg2-binary>=2.9.0

# Environment variables and settings
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# PDF parsing (for resume extraction)
pypdf>=3.17.0
PyMuPDF>=1.23.0  # Fast text extraction

# Web scraping (for job description extraction)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.1.0

# Search engine integration (for company research)
langchain-tavily>=0.1.0

# HTTP client for async requests
httpx>=0.25.0

# PDF generation (for writer agent)
weasyprint>=67.0

# Word document generation (for writer agent)
python-docx>=1.1.0

# Optional: LangSmith tracing (for debugging)
# langsmith>=0.1.0

# Optional: Additional LangChain integrations
# langchain-google-genai>=1.0.0  # For Google Gemini
# langchain-anthropic>=0.1.0     # For Claude

